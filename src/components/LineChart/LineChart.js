import useData from "../../hooks/useData";


const LineChart = () => {
    const data = useData();
    console.log(data);
    return (
        <div>

        </div>
    );
};

export default LineChart;